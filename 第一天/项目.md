介绍

云之家项目
1、我负责了些什么
	1、我负责通讯录模块的开发和维护，同时也会交叉着维护其他一些模块的内容
	2、我负责选人模块的开发、重构和维护

2、在我负责的事项中，我是怎么做的
	1、我认为通讯模块的开发的重点在于本地数据的读取需要非常的快速，不仅是在本模块中读取数据，还包括把数据提供给其他的模块
	2、通讯录的数据作为整个App基础功能的根本，整个数据从网络上获取的时候，也需要很快速和及时。
	3、选人模块的开发重点就是要保证用户在操作是，不能有卡顿出现，同时也需要支持各种场景下的选人业务

3、我遇到了什么问题
	1、我在开发通讯录模块时，遇到过一个问题：我们通过行为日志发现有个企业，在用户点击企业通讯录时，会比较耗时，这个企业大概有10万的员工。
	2、我在维护老的选人模块时，发现几个问题，
		a：对于不同业务场景下，不能快速的对接选人业务，要完成一个特定场景下的选人需求，可能需要入侵到选人模块里面才能完成；
 		b、在选择每一个人员时，对压入栈的所有界面都会回调进行刷新
		c、在有个场景下的会很卡顿
	3、启动App后，我快速在几个tab上点击时，app不能快速的响应我的点击事件


4、我是怎么解决的
	1、在发现有10万条数据接口缓慢时，我推动了后台对接口和获取企业人员数据方案进行改造
		1、首次点击企业通讯录时，没有必要就进行全量请求，就把网络请求改造成根据部门部门id进行分节点进行请求，
			同时对获取企业全量的人员接口的返回改成数据流的方式，不在是json的方式，把数据流转成本地文件，文件里面只记录一些基本的信息，比如说id，名称， 手机号，头像，部门id，部门名称，10万条数据 然后从文件里面读取逐行读取企业人员的相关信息。同时在首页数据显示完之后开启异步线程进行预请求数据。
		在数据有变更时，采用增量更新的方式进行数据请求，这时候采用是数据格式是json，在登录成功后，会在同步版本好的接口里面有相应模块，相应功能的的版本号，这时候，发现返回的版本比当前的要大，就直接请求，否则不做请求
	2、在老的选人模块的维护和业务对接中，因为不能够快速对接业务，在多次跟组长沟通之后，进行了改造
		1、先完成了适应不同场景的改造，我采用的方案是 外部配置，内部根据配置来组合构建首页和数据。我还采用了分级设计：
			a、首先是为了对老业务代码改的少，我先封装一层的统一入口层，这样老业务更替的时候，改动的就小
			b、可自定义首页数据来完成不同场景下的首页数据需求 
			c、采用工厂模式来配置和构建不同场景下的数据展示需求
			d、用协议的方式来对不同需求下的数据展示区域进行数据填充
		这样做是为了减少对选人主界面的代码入侵和提高选人主界面的灵活性
		2、在每次选人时候不在进行栈内控制器视图的刷新，统一到开始展示时候进行视图刷新
		3、在组件内，为了让数据处理的快速一些，采用了空间换时间的方式，在去重、判断是否存等操作上面采用Map方式快速查询
	3、我快速点击tab时，App不能够快速响应我的点击事件这是因为在主队列做的事情太多了，导致处理点击事件延迟
		因为后面几个tab是首次加载，快速点击时，都会走一次完整的界面加载逻辑，我就分析和检查了一些主要方法内的业务逻辑，把能在子线程做的事情，都放到了子线程，最重要的是数据获取逻辑，因为我们本地数据库逻辑还是采用的FMDB,FMDatabaseQueue还是采用的是同步逻辑，这中情况下还是会阻塞主队列

5、 这样做完之后，有什么样的结果
	1、在改造完企业人员数据获取方式之后，之前的用户习惯或有些改变，在一层一层点击下去时候，会进行网络数据获取，需要等待一小会，之前是只会在首次点击的时候进行网络请求和卡顿，这样做完的好处就是提高App的响应速度， 也保证了用户能及时看到数据
	 2、在我把老业务替换成新的选人模块之后，选人模块出现的bug也少了，整个选人过程中App也不会再出现等待现象了， 选人模块也更加轻量化了；在后续的业务接入时，只需要配置下参数组合就能快速完成，就算是全新的业务需求也只需要配置参数，回塞数据，定制UI规则，就能快速完成，同时整个模块的代码结构也更清晰了。

6、 后续这些是怎么进行维护的
	做完这些之后，不需要进行大量的维护，只需要按照指定好的方案进行常规的维护和开发

快乐平安项目

1、我负责了些什么
	1、组件化
	2、包大小优化
	3、启动优化
    
2、在我负责的事项中，我是怎么做的
	1、组件化：
		因为工程是很老的工程，每个业务都杂糅在一个工程下，业务之间耦合比较严重，导致在开发过程中需要大量的时间来进行多业务沟通。
		在充分了解整个项目相关联的业务之后，我开始了组件化方案的设计上。
	1、首先了解下业界的一些组件化方案，比如说url-block方案、Protocol-class方案、target-action方案，也对比了这些方案的优缺点，最终我根据需求选择了团队内部选择Protocol-class方案。选择这几个的原因是 a、在团队内部，我需要在编译过程中就呈现出错误和警告  b、团队内部需要传递的参数可以是遵循某个协议的对象，增加参数的灵活性  c、解耦代码量少，维护方便  d、避免硬编码
	2、确定好解耦方案之后开始做业务拆分，我们把原先的整个工程按照业务进行拆分，对业务用pod库进行管理，
在拆分各个业务时，同时把各个共用部分进行抽离并下沉，这样就形成了上下分层的工程化结构
	3、在业务组件通讯时，都需要依赖 中间层，中间层就是分装和管理Protocol-class层，中间层不依赖任何非系统库。
	
3、我遇到了什么问题
	1、组件化解耦过程中，有次发现工程代码在打包机上编译不过，看其原因是组件库的podspec文件中的依赖形成了环状，导致pod报错。
	2、在组件化过程中，发现同事把protocol的头文件放的位置不正确，存放在了自己组件内部，这个是不允许的，而是应该放到中间层自己的模块文件夹下面，这导致在业务的pod组件库内需要单独抽离一个subspec来存放这个Protocol协议文件
	3、在组件化过程中，并没有做的独立组件的能够独立编译，这个虽然有做要求，但还是有些组件库没有做到，我是组件化的负责人，我每天晚上时候都会对组件库进行独立编译
	// 4、在完成Pod组件化后，过了段时间之后，我们发现包大小增加了不少，快要到达警戒线了150MB了
		
4、我是怎么解决的
	1、在解决pod的循环依赖时，因为组件库过多，导致花费了一些时间，最终找到了2个组件库都依赖的文件，并将其下沉之后，即可解决该问题
	2、3问题、我拉着相关业务的同事，和相关组件化的人员进行整理，重新讲解了下Protocol文件的存放位置，以及为什么放这里，并再次要求单独组件库必须能编译过的原则。

5、 这样做完之后，有什么样的结果
	1、	组件化做完之后的结果有好的结果和不好的结果：好的结果就是，整个项目结构化更加清晰，分工更为明确，提高了业务直接的开发对接过程，同时也带来了一些不好的结果：1、由于组件库比较多，增加了部分同事代码合并时候的工作量，因为在封板的时候，需要把所有由于业务开发涉及到的库都需要进行打tag和存档，这时候就需要频繁的切换，增加了一些工作量  2，会相应的增加一些代码量

6、 后续这些是怎么进行维护的
	组件化完成后，没有多少维护的工作量


2、包大小优化
##### 关注点：各个阶段的耗时比：
找到重点之后进行相对的去优化：都需要进行记录的数值
	1、在我负责的事项中，我是怎么做的
		包大小优化是在组件化完成之后开始着手去做的。
		首先，我去搜索了些影响包大小的一些方案和去了接整个ipa的结构，我这这个过程中，我去找了一些可能影响生成的ipa大小的因素：
大致有2类，就是资源和代码，
		然后：我就开始分为三步进行处理 ，1，是对Xcode的设置进行配置，比如说C++的异常支持，设置构建的cup架构，一些strip相关的设置 ，这些在release下都可以进行调整，，xcode的这些设置是一次性就好了，不像资源和代码，需要持续的进行
		2、是对资源进行处理：处理方式包括删除无用资源和对资源进行压缩，我们先做了对无用资源的删除，这边包括但不限于图片资源，json文件，音视频等资源
		3、对代码的一些处理：1、通过 otool 工具对ipa中的可执行文件进行dump出其中的 __objc_class_list 段和 __objc_class_refs段做差集找到未使用的 Objc 类， 2、通过linkmap文件梳理出所有类和方法，通过otool工具可dump可执行文 __objc_selrefs 中所有使用的方法
	3、我遇到了什么问题
		1、我遇到了一些同事在做需求时，对需要带入的一些资源不会进行压缩
		2、在组件化过程中，对资源采用是assets用来统一处理，而不是将一个一个的图片放置到assets目录下，这导致了有，打包出来的ipa中会存在2份该组件生产的assets.car文件包
		3、在查找无用的类时，会存在一些误判的情况，通过字符串生成的类，统计不到，一些基类会认为是无用的类，通过运行时动态派生的一些类也会被认为是没有用到，还有一些类只有在load方法和intialize方法时，也会误判为无用类，这些误判导致在查找时，需要确认的工作量加大。
		4、在查找无用方法时，会出现更多需要二次确认的方法，1、除了字符串动态调用的，还有performtoselector相关的，还有就是 kvc 调用的一些属性方法
	4、我是怎么解决的

组件化
1、一些组件化的方案
2、这些方案之间的优缺点
3、如何在项目中选择组件化方案


5、app组件化架构和多方案对比
6、flutter通信
8、https原理和场景处理
9、app性能优化多方案对比
10、网络场景题解决方案
11、flutter热更新
12、内存管理和多线程技术
13、穿插部分算法，队列，排序，二分等

大概率还是会从项目聊起，同样会看项目的亮点难点、再延伸至你自己的优劣势，以及项目上涉及到的技术点，延伸去聊~ 另外ios的基础、底层原理、组件化相关的，也需要多看下
一面的，内存管理，多线程，锁，然后项目里的技术点
二面，mvvm，mvp架构，http协议，项目问题
三面，围绕项目来问的，关注技术难度，这个项目为什么这么做，还有没有其他解决方案


1、通知中心 底层数据结构
2、

moya

category 的装载  内存里面放置哪里
mach-o ->段


常见面试问题
1、工作中遇到的问题或失败案例：为什么会失败？当时你是怎么处理的？如果换成现在的你再去处理这个问题你会怎么做？
2、项目：先了解候选人是参与项目的过程还是负责整个项目的进行。
3、参与：需要说明在项目中负责哪个环节，学习到哪些，哪些需要优化，个人的收获是什么；
4、负责：需要表达从项目起因、经过和结果，全流程的情况，个人的见解是什么。
5、每段工作离职的原因，尤其是跳槽频繁的情况和最近两段工作离职的原因，请候选人提前准备好，尽量表达得合理化一些。


1、说清楚项目上的事情 
	1、找个亮点 
		挑战	
			遇到的问题
				怎么处理的
				有没有其他方案
				后续的维护

不需要着急回答
	
	

如果有音视频经验


提给他们的问题：
	
1：项目上情况
2：部门 、业务
3：发展